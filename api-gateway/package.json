{
  "type": "module",
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.5.0",
    "fastify-cli": "^7.4.0"
  },
  "scripts": {
    "start": "npx fastify start server.js --watch -l info -a 0.0.0.0"
  }
}
